package com.example.demo.repository;

import com.example.demo.domain.IdentitySkill;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IdentitySkillRepository extends JpaRepository<IdentitySkill, Long> {
}